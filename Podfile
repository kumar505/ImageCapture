# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ImageCapture' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageCapture

  # Pod for toast messages
  pod 'Toast-Swift', '~> 4.0.0'

  # Pod for debug logging
  pod 'XCGLogger', '~> 6.1.0'

  # Pod for showing spinners
  pod 'MBProgressHUD', '~> 1.1.0'

  # Pod for networking
  pod 'Alamofire', '~> 4.7'

  target 'ImageCaptureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImageCaptureUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end